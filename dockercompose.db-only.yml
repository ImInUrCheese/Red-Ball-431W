services:
  db:
    image: mysql:9.6.0
    cap_add:
      - SYS_NICE
    restart: always
    environment:
      - MYSQL_DATABASE = db
      - MYSQL_ROOT_PASSWORD = secret
    volumes:
      - mysql_data_dev:/var/lib/mysql

volumes:
  mysql_data_dev: